name: format-rust-code
on:
  pull_request:
    branches: [main]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Format Rust Code
        uses: flowscripter/.github/actions/format-rust-code@v1
